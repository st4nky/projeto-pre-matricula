import React from 'react';
import ReactDOM from 'react-dom';
import ProjetoSituacao from './ProjetoSituacao'; // Atualize a importação
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
    <React.StrictMode>
        <ProjetoSituacao />
    </React.StrictMode>,
    document.getElementById('root')
);

reportWebVitals();
